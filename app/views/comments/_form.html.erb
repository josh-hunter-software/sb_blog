<%= simple_form_for(@comment) do |f| %>

  <div class="form-inputs">
    <%= f.input :content %>
    <%= f.association :post %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
